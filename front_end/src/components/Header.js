import React from 'react';
import '../App.css';

function Header(){
    return(
        <div className = "Header bg-dark">
            <h1 className = "text-light">This is the header</h1>
        </div>
    );
}

export default Header;